#!/bin/bash
#HOSTNAME Boot

cd PWD
sh -c 'screen -dmS bl_manager -d -m PYTHON PWD/bluetooth_agent.py; exec bash'&
sh -c 'screen -dmS fs_check -d -m PYTHON PWD/check_fs.py -w WFILE -b BLFILE -t DEL &> /tmp/check_fs.log; exec bash'&
exit 0
